const ENCODING_STR = 'imba!';
let NEXT_UUID = 0;

/*
 * TODO define new Admin(params) class:
 * Public properties:
 * + uuid (generated using private static property NEXT_UUID)
 * + login
 * + password
 * Public methods:
 * + getLogin()
 * + setLogin(value)
 * + getEncodedPassword()
 * Private methods:
 * - encodePassword() (encoded using private static property ENCODING_STR)
 * Private static properties:
 * - NEXT_UUID
 * - ENCODING_KEY
 * Private static methods:
 * - generateUuid() (generates next uniq UUID)
 * */
